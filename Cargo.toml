[workspace]
resolver = "3"
members = [
    "samp-codegen",
    "samp-sdk",
    "samp",
    "examples/hello",
    "examples/counter",
    "examples/advanced",
]

[profile.release]
lto = "thin"
codegen-units = 1
strip = true